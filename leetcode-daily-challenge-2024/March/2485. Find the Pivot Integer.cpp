class Solution
{
public:
    int pivotInteger(int n)
    {
        vector<int> arr;
        for (int i = 1; i < n; i++)
        {
            arr.push_back(i);
        }
        int i = 0;
        if(arr[i]==arr[n]+i-arr[i]
           return i;
        else return -1;
    }
};
